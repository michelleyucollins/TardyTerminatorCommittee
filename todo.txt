- Need to finish cleaning the names of the data
- Filter out the SRT line, as it is no longer in use
- Make a new processed file SYSTEM and create the following data
    - Frequency (Has all of the data)
    - Time (Only use the data that have minutes delay and minutes gap not equal to zero)
    - Cause of data (Keep only the data with a valid error code)

- Then create a python data analysis file, which supports analyzing:
    - delays by station (frequency)
    - delays by station (time)
    - delays by day of week / time of day
    - delays by causes
    - delays by causes for certain stations